# API 索引

当前仓库保留的主机侧模块主要用于接口验证和上层业务代码整理，不代表板上 Python VM 已经可用。

主要模块：

- `maix`
- `maix.GPIO`
- `maix.camera`
- `maix.display`
- `maix.nn`
- `maix.spi`
- `maix.i2c`
- `maix.uart`
- `maix.pwm`
- `maix.adc`
- `maix.pinmap`

当前 API 的真实板级能力边界，以固件启动后的 `maix_info` 输出为准。
